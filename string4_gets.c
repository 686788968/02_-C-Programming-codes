#include <stdio.h>
#include <string.h>

int main()
{

    char st[30];
    gets(st);
    puts(st); // this function print the string and puts the cursor on next line
    printf("Jat");  //printed on next line as because puts moves the cursor on next line like as \n

    return 0;
}